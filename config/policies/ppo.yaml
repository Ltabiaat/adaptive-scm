# Override fragment for the PPO agent. Composed with config/default.yaml.
total_timesteps: 500000
clip_range: 0.2
gamma: 0.99
gae_lambda: 0.95
learning_rate: 0.0003
batch_size: 64
n_epochs: 4
n_steps: 2048
policy_net: [64, 64]
activation: tanh
